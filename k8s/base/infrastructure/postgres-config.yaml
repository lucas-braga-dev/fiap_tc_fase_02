apiVersion: v1
kind: Service
metadata:
  name: postgres-config
  labels:
    app.kubernetes.io/name: postgres-config
    app.kubernetes.io/component: database
    app.kubernetes.io/part-of: togglemaster
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: postgres-config
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-config
  labels:
    app.kubernetes.io/name: postgres-config
    app.kubernetes.io/component: database
    app.kubernetes.io/part-of: togglemaster
spec:
  serviceName: postgres-config
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: postgres-config
  template:
    metadata:
      labels:
        app.kubernetes.io/name: postgres-config
        app.kubernetes.io/component: database
        app.kubernetes.io/part-of: togglemaster
    spec:
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 30
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: postgres
          image: postgres:16-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - name: postgres
              containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: postgres
            - name: POSTGRES_USER
              value: config_user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: togglemaster-secrets
                  key: CONFIG_DB_PASSWORD
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          startupProbe:
            exec:
              command: ["pg_isready", "-U", "config_user", "-d", "postgres"]
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 30
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "config_user", "-d", "postgres"]
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "config_user", "-d", "postgres"]
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
            - name: init
              mountPath: /docker-entrypoint-initdb.d
              readOnly: true
            - name: schemas
              mountPath: /opt/togglemaster
              readOnly: true
      volumes:
        - name: init
          configMap:
            name: postgres-config-init
            items:
              - key: 01-init.sql
                path: 01-init.sql
        - name: schemas
          configMap:
            name: postgres-config-init
            items:
              - key: flags.sql
                path: flags.sql
              - key: targeting.sql
                path: targeting.sql
  volumeClaimTemplates:
    - metadata:
        name: data
        labels:
          app.kubernetes.io/name: postgres-config
          app.kubernetes.io/part-of: togglemaster
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
